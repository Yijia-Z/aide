

所有的interface都在/models.py
all interface are in models.py

所有api都在/api/一个名称下包含对应的所有API操作
all apis are in api dir. 
the file name means all apis operations in the file.

所有需要的function都在/service/
all helper function in the service dir
所有run tool function都在/function/
all run tool function in function dir(will move later since we have new solution for this part)